# ==============================
# 🔧 CONFIGURAZIONE DATABASE (Railway)
# ==============================

spring.datasource.url=jdbc:mysql://mysql.railway.internal:3306/railway
spring.datasource.username=root
spring.datasource.password=csQNzvdyaLgRmzQACRpvLyfeoHUfHqFk
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==============================
# ⚙️ CONFIGURAZIONE JPA / HIBERNATE
# ==============================
# Crea o aggiorna automaticamente le tabelle
spring.jpa.hibernate.ddl-auto=update

# Mostra le query SQL nella console (facoltativo)
spring.jpa.show-sql=true

# Dialect per MySQL 8+
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ==============================
# 🌐 CONFIGURAZIONE SERVER
# ==============================

# Railway assegna una porta dinamica → leggiamola da variabile d'ambiente
server.port=${PORT:8080}

# (Facoltativo) Profilo di esecuzione
spring.profiles.active=prod
